# Text Strip Alfred workflow
Strip any leading and trailing whitespace from text.

## Usage
Strip text using the `strip` keyword followed by the text. The stripped text is automatically copied to your clipboard.
